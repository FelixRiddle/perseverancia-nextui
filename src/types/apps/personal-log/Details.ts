export interface GeneralDetails {
	subtype: string;
}

export interface EmptyDetails {
}

export interface ProgrammingDetails {
	appName: string;
	language: string;
	framework: string;
	repositories: string[];
}
